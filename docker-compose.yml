version: '3.8'

services:
  hr-tools:
    build: .
    ports:
      - "3001:3001"
    environment:
      - HR_TOOLS_PORT=3001
      - GROQ_API_KEY=${GROQ_API_KEY}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://localhost:11434}
    volumes:
      - .env:/app/.env:ro
    restart: unless-stopped